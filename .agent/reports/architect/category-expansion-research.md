# 📋 Архитектурное исследование: Расширение категорий HardwareLab

**Роль:** Исследователь-Архитектор  
**Дата:** 2026-01-08  
**Статус:** ✅ Завершено

---

## 🎯 Цель исследования

Спроектировать архитектуру для расширения HardwareLab с 4 до 7 категорий с поддержкой специализированных компонентов и multi-ASIN сборок.

---

## 📊 Анализ текущего состояния

### Схема контента (`src/content/config.ts`)
| Поле | Тип | Ограничение |
|------|-----|-------------|
| `asin` | `string` | **Только 1 ASIN** |
| `priceCategory` | `enum` | `budget`, `mid`, `high`, `enterprise` |
| `tags` | `string[]` | Используется для категоризации |

### Существующие компоненты (12 шт)
| Компонент | Назначение | Переиспользуемость |
|-----------|------------|-------------------|
| `ReviewHero` | Hero-секция с рейтингом | ✅ Универсальный |
| `SpecGrid` | Таблица характеристик | ✅ Универсальный |
| `ProsCons` | Плюсы/минусы | ✅ Универсальный |
| `UserFeedback` | Отзывы пользователей | ✅ Универсальный |
| `AffiliateButton` | Кнопка Amazon | ⚠️ Только 1 ASIN |

### Текущие категории
Категории определены **hardcoded** в `[category].astro`:
- `mini-pc`, `consoles`, `nas`, `sbc`

---

## 🔍 Выявленные проблемы

1. **Single-ASIN ограничение** — невозможно создать обзор сборки с несколькими компонентами
2. **Hardcoded категории** — добавление новых категорий требует правки кода
3. **Отсутствие типизации категорий** — нет схемы для мета-данных категорий
4. **Нет специализированных компонентов** — для мониторов, AI workstations и т.д.

---

## 🏗️ Предлагаемая архитектура

### 1. Расширение схемы контента

```typescript
// Новые поля для reviews
reviewType: z.enum(['standard', 'build']).default('standard'),
category: z.enum(['gaming', 'monitors', 'ai-workstation', 'mini-pc', 'nas', 'sbc']),

// Для Build-типа (multi-ASIN)
buildComponents: z.array(z.object({
  name: z.string(),
  asin: z.string(),
  category: z.enum(['cpu', 'gpu', 'ram', 'storage', 'case', 'psu', 'cooling']),
  price: z.number().optional(),
  alternativeAsin: z.string().optional()
})).optional(),

// Категорийно-специфичные поля
monitorSpecs: z.object({...}).optional(),
gamingPerformance: z.object({...}).optional(),
aiPerformance: z.object({...}).optional()
```

### 2. Новые компоненты

| Компонент | Категория | Назначение |
|-----------|-----------|------------|
| `BuildHero.astro` | AI Workstations (Build) | Hero с общей ценой и списком компонентов |
| `ComponentsGrid.astro` | Build | Таблица компонентов с отдельными ASIN |
| `AlternativeParts.astro` | Build | Альтернативные комплектующие |
| `MonitorSpecs.astro` | Monitors | Характеристики: разрешение, частота, панель |
| `GamingPerformance.astro` | Gaming | FPS в играх, поддержка технологий |
| `AIPerformance.astro` | AI Workstations | ML-бенчмарки, VRAM, compute capabilities |

### 3. URL-структура

```
/reviews/{slug}                    # Standard reviews
/builds/{slug}                     # Build guides (multi-ASIN)
/categories/{category}/            # Category listings
```

---

## 📁 Структура папок для отчетов агентов

```
.agent/
├── reports/
│   ├── architect/           # Отчеты архитектора
│   │   └── category-expansion-research.md  (этот файл)
│   ├── coder/               # Отчеты кодера
│   │   └── [task-name]-completion.md
│   └── reviewer/            # Отчеты ревьювера
│       └── [task-name]-review.md
└── workflows/               # Существующие workflow
```

---

## 📎 Ссылки на документы

- **Текущая схема:** `src/content/config.ts`
- **Компоненты:** `src/components/ui/`
- **Категории:** `src/pages/categories/[category].astro`
- **Обзор проекта:** `.agent/project-overview.md`
