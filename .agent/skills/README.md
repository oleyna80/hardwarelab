# Project Skills

Bootstrap copies the core framework skills into this directory for local agent
routing.

Claude Code also receives the same skill set under `.claude/skills/` because
that is its runtime skill location. Keep the two directories aligned when you
add or remove skills.
