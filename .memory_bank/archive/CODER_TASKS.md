# 👨‍💻 ЗАДАНИЕ ДЛЯ КОДЕРА (DEV режим)

**Приоритет:** 🟡 СРЕДНИЙ  
**Среда:** WSL (Development)  
**Цель:** Подготовить проект для комфортной разработки

---

## 🎯 Задачи для DEV-режима

### ✅ Задача 1: Настроить environment variables

**Зачем:** Убрать hardcoded `YOUR_TAG-20` из кода

**Шаги:**

1. Создать `.env` в корне проекта:
```bash
cd /home/dmitrii/projects/Amazon_aff/hardwarelab
touch .env
```

2. Добавить в `.env`:
```env
# Development Amazon Tags (для тестирования)
# Замените на реальные перед деплоем
AMAZON_TAG_US=devtag-20
AMAZON_TAG_UK=devtag-21
AMAZON_TAG_DE=devtag-03
AMAZON_TAG_FR=devtag-21

# Site URL (для dev)
PUBLIC_SITE_URL=http://localhost:4321
```

3. Добавить `.env` в `.gitignore`:
```bash
echo "" >> .gitignore
echo "# Environment variables" >> .gitignore
echo ".env" >> .gitignore
echo ".env.local" >> .gitignore
echo ".env.*.local" >> .gitignore
```

4. Обновить `src/components/ui/AffiliateButton.astro`:

**Найти строку 37:**
```typescript
tag = 'YOUR_TAG-20',
```

**Заменить на:**
```typescript
tag = import.meta.env.AMAZON_TAG_US || 'devtag-20',
```

**Примечание:** Для продакшн нужно будет добавить логику выбора тега по региону.

---

### ✅ Задача 2: Исправить rel атрибуты

**Файл:** `src/components/ui/AffiliateButton.astro`

**Найти строку 108:**
```astro
rel="sponsored nofollow noopener noreferrer"
```

**Заменить на:**
```astro
rel="nofollow sponsored noopener noreferrer"
```

**Почему:** Правильный порядок для SEO и compliance.

---

### ✅ Задача 3: Создать .env.example

**Зачем:** Чтобы другие разработчики знали, какие переменные нужны

**Создать файл `.env.example`:**
```env
# Amazon Affiliate Tags
# Get your tags from: https://affiliate-program.amazon.com
AMAZON_TAG_US=your-us-tag-20
AMAZON_TAG_UK=your-uk-tag-21
AMAZON_TAG_DE=your-de-tag-03
AMAZON_TAG_FR=your-fr-tag-21

# Site URL (change for production)
PUBLIC_SITE_URL=http://localhost:4321
```

**Этот файл МОЖНО коммитить в git** (он без реальных значений).

---

## 📋 Чеклист выполнения

- [ ] Создан `.env` файл
- [ ] `.env` добавлен в `.gitignore`
- [ ] `AffiliateButton.astro` использует `import.meta.env.AMAZON_TAG_US`
- [ ] Исправлен порядок `rel` атрибутов
- [ ] Создан `.env.example`
- [ ] Перезапущен dev server для применения env variables

---

## 🧪 Тестирование

После выполнения:

```bash
# 1. Перезапустить dev server
# Ctrl+C в терминале с npm run preview
npm run dev

# 2. Открыть в браузере
# http://localhost:4321

# 3. Проверить affiliate ссылку
# Открыть любой review
# Inspect элемент кнопки "Check Price on Amazon"
# Проверить href содержит: ?tag=devtag-20
# Проверить rel="nofollow sponsored noopener noreferrer"
```

---

## 📝 Заметки для будущего деплоя

Когда будете готовы к деплою, нужно будет:

1. **Получить реальные Amazon Associate Tags:**
   - Зарегистрироваться на https://affiliate-program.amazon.com
   - Получить теги для каждого региона (US, UK, DE, FR)
   - Обновить `.env` с реальными тегами

2. **Настроить production env variables:**
   - В Netlify/Vercel добавить env variables
   - Обновить `PUBLIC_SITE_URL` на реальный домен

3. **Добавить остальные компоненты:**
   - `AffiliateDisclosure.astro` (above the fold)
   - Sitemap integration
   - Schema.org разметка
   - hreflang теги

Но это всё **потом**, когда будете готовы к деплою!

---

## ✅ Готово!

После выполнения этих задач:
- ✅ Код будет чище (нет hardcoded значений)
- ✅ Легче тестировать affiliate ссылки
- ✅ Готово к добавлению в git
- ✅ Легко поделиться с другими разработчиками

**Время выполнения:** ~5 минут

Хотите, чтобы я выполнил эти задачи прямо сейчас?
