services:
  app:
    build: .
    image: hardwarelab-site:local
    ports:
      - "8081:4321"
    environment:
      HOST: 0.0.0.0
      PORT: 4321
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
